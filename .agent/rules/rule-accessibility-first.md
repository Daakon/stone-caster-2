---
trigger: always_on
---

All new UI components and features must be accessible. This includes using semantic HTML, providing `aria-` attributes where necessary, ensuring keyboard navigability, and including labels for all form controls. When modifying UI, verify that accessibility is not degraded.