---
trigger: always_on
---

You have MCP read-only access to the Supabase Postgres database.

Before proposing schema changes or queries, inspect the current DB objects via MCP when feasible:
- tables, columns, types
- constraints, indexes
- RLS policies
- views/functions/triggers
