Before beginning implementation, you must analyze the user's request to determine the primary scope of the task (e.g., 'backend API authentication', 'frontend character sheet UI'). List the core files and directories you expect to modify based on this scope.

If your implementation plan requires touching files outside this initial list, you MUST present these 'out-of-scope' changes to the user. Explain why they are necessary and get explicit approval before proceeding. This prevents unintended side effects and ensures all changes are focused and deliberate.

For example, if a request to change UI text unexpectedly requires a change to a backend API schema, that is an out-of-scope change that needs approval.