---
trigger: always_on
---

After making any code changes and before signaling completion, you must run the relevant linting and type-checking commands for the workspace you modified (e.g., `npm run lint` and `npm run typecheck` in `/backend` or `/frontend`). All errors must be fixed.