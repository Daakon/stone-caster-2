---
description: Add New Dependency
---

Before adding a new dependency, you must: 1. Verify it's necessary and a similar utility doesn't already exist in the project. 2. Analyze its bundle size and maintenance status. 3. Add the package to the correct workspace (`frontend` or `backend`). 4. After installation, run `npm audit` to check for vulnerabilities and report any high or critical issues.