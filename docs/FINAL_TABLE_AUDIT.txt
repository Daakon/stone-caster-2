FINAL TABLE AUDIT - Other Tables
Generated: 2025-12-04
Status: READY TO EXECUTE

================================================================================

SQL SCRIPT TO EXECUTE:
======================

Copy and paste the following SQL into your Supabase Dashboard SQL Editor:

/*
  List table names that are NOT 'chimera_%', NOT 'awf_%', NOT 'stone_%', NOT 'mod_%'.
  Exclude standard Supabase schemas (auth, storage, realtime, graphql, vault).
  
  Purpose: Identify remaining tables that may be legacy candidates for deletion.
  Date: 2025-12-04
*/

SELECT 
  schemaname, 
  relname as table_name,
  n_live_tup as row_count,
  pg_size_pretty(pg_total_relation_size(schemaname||'.'||relname)) as table_size
FROM pg_stat_user_tables 
WHERE schemaname = 'public'
  AND relname NOT LIKE 'chimera_%'
  AND relname NOT LIKE 'awf_%'
  AND relname NOT LIKE 'stone_%'
  AND relname NOT LIKE 'mod_%'
  -- Exclude common system tables if they appear in public (pg_*)
  AND relname NOT LIKE 'pg_%'
  AND relname NOT LIKE 'sql_%'
ORDER BY relname;

-- Summary count
SELECT 
  COUNT(*) as total_other_tables
FROM pg_stat_user_tables 
WHERE schemaname = 'public'
  AND relname NOT LIKE 'chimera_%'
  AND relname NOT LIKE 'awf_%'
  AND relname NOT LIKE 'stone_%'
  AND relname NOT LIKE 'mod_%'
  AND relname NOT LIKE 'pg_%'
  AND relname NOT LIKE 'sql_%';

================================================================================

EXECUTION INSTRUCTIONS:
=======================

OPTION 1: Supabase Dashboard (RECOMMENDED - Easiest)
------------------------------------------------------
1. Go to https://supabase.com/dashboard
2. Select your project
3. Click on "SQL Editor" in the left sidebar
4. Click "New query"
5. Copy and paste the SQL script above
6. Click "Run" (or press Ctrl+Enter)
7. Copy the results and paste them below this line
8. Save this file

OPTION 2: Using psql Command Line
----------------------------------
If you have psql installed and DATABASE_URL set:

  psql $DATABASE_URL -f backend/scripts/inspect_other_tables.sql > docs/FINAL_TABLE_AUDIT.txt

Or with explicit connection string:

  psql "postgresql://user:password@host:port/database" -f backend/scripts/inspect_other_tables.sql > docs/FINAL_TABLE_AUDIT.txt

OPTION 3: Using Supabase CLI
-----------------------------
If you have Supabase CLI installed:

  supabase db execute --file backend/scripts/inspect_other_tables.sql > docs/FINAL_TABLE_AUDIT.txt

================================================================================

EXPECTED OUTPUT:
===============

The query will return:
- A list of all tables in the public schema that don't match the excluded patterns
- Each row will show: schema name, table name, row count, and table size
- A final count of total "other" tables

The script filters out:
- chimera_* (Chimera V3 tables - KEEP)
- awf_* (legacy AWF tables - already marked for deletion)
- stone_* (legacy Stone tables - already marked for deletion)
- mod_* (legacy Mod tables - already marked for deletion)
- pg_* (PostgreSQL system tables)
- sql_* (SQL system tables)

================================================================================

RESULTS (Paste output here after execution):
============================================

[Paste SQL query results here]

================================================================================
